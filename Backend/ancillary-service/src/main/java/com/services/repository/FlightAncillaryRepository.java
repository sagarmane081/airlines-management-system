package com.services.repository;

import com.services.entity.FlightAncillary;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FlightAncillaryRepository extends JpaRepository<FlightAncillary, Long> {
}
